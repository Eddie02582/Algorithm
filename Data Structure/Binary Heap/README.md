# 📦 Binary Heap 

Binary Heap 是一種 **完全二元樹 (Complete Binary Tree)**，支援：

- **最小堆 Min-Heap**：父節點 ≤ 子節點  
- **最大堆 Max-Heap**：父節點 ≥ 子節點  

用途：

- 優先隊列 (Priority Queue)
- Top-K 問題
- Heap Sort
- 動態取得最大/最小值

---

## 1️⃣ 插入節點 (Insert)

假設已有最小堆：

```
          1
        /   \
       3      2
      / \    / \
     6   5  7   8
    / \  /
   9  10
```

新增節點 `0`，放在最左下的空位：

```
          1
        /   \
       3      2
      / \    / \
     6   5  7   8
    / \  /
   9  10 0*
```

### 上浮（Sift Up）

1. 父節點 `5` > 0 → 交換  
2. 父節點 `3` > 0 → 交換  
3. 父節點 `1` > 0 → 交換  

最終堆：

```
          0
        /    \
       1      2
      / \    / \
     6   3  7   8
    / \  /
   9  10 5
```

> ✅ 維持最小堆性質

---

## 2️⃣ 刪除節點 (Delete Min)

刪除堆頂 `1`：

1. 將最後節點移至堆頂：

```
          10
        /    \
       3      2
      / \    / \
     6   5  7   8
    /   
   9
```

2. 下沉（Sift Down）：

- 與最小子節點 `2` 交換  

```
          2
        /    \
       3      10
      / \    / \
     6   5  7   8
    /   
   9
```

- 再與最小子節點 `7` 交換  

```
          2
        /    \
       3      7
      / \    / \
     6   5  10  8
    /   
   9
```

> ✅ 維持最小堆性質

---

## 3️⃣ 建構 Binary Heap (Heapify)

初始無序完全二元樹：

```
          7
        /    \
       1      3
      / \    / \
     10  5  2   8
    /  \ 
   9    6
```

從最後一個非葉子節點開始下沉：

1. 節點 `10` 下沉 → 與子節點最小 `6` 交換  

```
          7
        /    \
       1      3
      / \    / \
     6   5  2   8
    / \ 
   9  10
```

2. 節點 `3` 下沉 → 與最小子節點 `2` 交換  

```
          7
        /    \
       1      2
      / \    / \
     6   5  3   8
    / \ 
   9  10
```

3. 節點 `1` 小於子節點 → 不動
4. 節點 `7` 下沉 → 與最小子節點 `1` 交換，再與 `5` 交換  

```
          1
        /    \
       5      2
      / \    / \
     6   7  3   8
    / \ 
   9  10
```

> ✅ 完成 Binary Heap 建構

---

## 4️⃣ Python 實作

```python
import heapq

# 初始列表
nums = [7, 1, 3, 10, 5, 2, 8, 9, 6]
heapq.heapify(nums)  # 建堆

# 插入
heapq.heappush(nums, 0)  # 自動上浮

# 刪除堆頂
min_val = heapq.heappop(nums)  # 自動下沉

# 取前 k 小元素
top3 = heapq.nsmallest(3, nums)
print(top3)
```

---

## 5️⃣ LeetCode Binary Heap 常見題目

| 題號 | 題名 | 難度 | 說明 |
|------|------|------|------|
| 215 | Kth Largest Element in an Array | Medium | Top-K |
| 703 | Kth Largest Element in a Stream | Easy | 優先隊列 |
| 23 | Merge k Sorted Lists | Hard | Heap 合併 K 列表 |
| 347 | Top K Frequent Elements | Medium | Counter + Heap |
| 1046 | Last Stone Weight | Easy | Max-Heap |
| 295 | Find Median from Data Stream | Hard | Min-Heap + Max-Heap |

---

## 6️⃣ 學習建議

1. 熟練 **插入 / 上浮** 與 **刪除 / 下沉**
2. 練習 **Heapify 建堆**
3. 掌握 Python **heapq 模組**
4. 結合 LeetCode Top-K / 優先隊列題目
5. 了解 Max-Heap / Min-Heap 差異與應用

---

✔ 完整 Binary Heap Markdown 教學（Python + 可視化 + LeetCode 範例）
